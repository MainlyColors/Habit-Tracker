{
  "name": "habit-tracker",
  "version": "0.0.1",
  "description": "App for tracking habit completion",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon",
    "dev:server": "cross-env NODE_ENV=development npm run ts:exec server/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts:exec": "ts-node -r tsconfig-paths/register --project ./tsconfig.json --transpile-only"
  },
  "author": "Ryan Mahara",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.0",
    "cross-env": "^7.0.3",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0"
  }
}
